<% 
	for(let key in vars)
	{
		let v = vars[key]

		if(v === undefined)
		{
			continue
		}

		%>var <%-key%> = <%

		if(typeof v === "string")
		{
			%>`<%-v%>`; <% 
		}

		else
		{
			%><%-JSON.stringify(v)%>; <%
		}
	}
%>