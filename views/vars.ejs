<% 
	for(var key in vars)
	{
		var v = vars[key]

		if(v === undefined)
		{
			console.error(`${key} is undefined`)
			continue
		}

		%><%-key%> = <%

		if(typeof v === "string")
		{
			%>`<%-v%>`; <% 
		}

		else
		{
			%><%-v%>; <%
		}
	}
%>